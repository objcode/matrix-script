#!/usr/bin/env python

from distutils.core import setup
import setuptools

setup(name='matrix',
      version='0.1',
      author='Sean McQuillan',
      author_email='sean@twilio.com',
      description='output filter that causes matrix spam',
      scripts=['matrix'])
